extrawindow is a library for open source programming language and environment processing (http://www.processing.org).
This library allows to  create and access multiple windows in one sketch. Each ExtraWindow extends PApplet and can be treated as such. This library can be downloaded from http://www.sojamo.de/libraries/extrawindow
for changes read the changeLog.txt file inside folder 'resources'.
this library is maintained by andreas schlegel, for questions, comments, suggestions contact andreas at andi@sojamo.de
